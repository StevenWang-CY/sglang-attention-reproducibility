# Data index — raw result dirs

The large raw measurement outputs are **not** moved into `page_size_study/`: they stay in the repo's canonical results location `offline_batch_results/`, which is **gitignored** (`offline_batch_results/*`, except the committed `H100-80GB-HBM3/`). That dir is also the rsync target the gray runs write to and the path every figure generator reads. This file catalogues what each dir is.

Path: all dirs below are under `offline_batch_results/`.

| dir | size | experiment | read by (script → report) |
|---|---|---|---|
| **`dense_5060ti/`** | 74 M | Dense `batch × page_size` re-run (radix ON/OFF), Qwen3-VL-2B — the MAIN sweep | `make_dense_figures.py`, `make_models_figures.py` → report 3 |
| **`gen_models_5060ti/`** | 17 M | Cross-model sweep (Qwen2.5-3B, Qwen3-VL-2B/4B/8B-FP8) + FlashInfer graph-ON long-ctx (`fgf_*`, `fgr_*`, `fgi_*`) | `make_models_figures.py`, `make_flashinfer_fullsweep_figure.py`, `make_flashinfer_graph_figure.py` → reports 3 & 4 |
| **`bench_one_batch_5060ti/`** | few M | **TRUE-batch** `bench_one_batch` data, CUDA graph ON, independent seqs → report 5. Main sweep: Qwen3-VL-2B page{1,8,32,128}×{triton,flashinfer}×6 (B,L)×2 rounds (`bob_r*_*.jsonl`). **Adversarial break-search:** `bobx_*` (2B fine-grid high-batch/L2-resident/long-seq), `bobm_<model>_*` (Qwen2.5-3B GQA-2 to batch 512, Qwen3-VL-4B), `bobg_*` (graph knobs). | `analyze_bench_one_batch.py`+`make_bench_one_batch_figure.py` (main); `analyze_bob_break.py`+`make_bob_break_figure.py` (adversarial) → report 5 |
| **`xqa_profile/`** | ~6 M | **XQA TMA profile + stack-wide TMA survey (report 6)** — `ncu` 2025.3.1 counters on **phastform** (sm120, root). (1) XQA{16,32,64,128}+FlashInfer{1,16,32,64,128} × batch{1,8,32,64} × ctx{1k,4k,16k} + full-set: `{be}_ps*_bs*_kv*.{ncu-rep,csv}`. (2) **Stack survey** `survey*.csv`: GEMM (cuBLAS bf16/fp16/fp8, fi cutlass/cudnn fp8), SDPA, fi prefill, Triton-TMA control, norm → which kernels use TMA. Finding: TMA only in CUTLASS-Blackwell GEMM/MoE; no attention. | `analyze_xqa_ncu.py`, `make_xqa_figure.py` → report 6 |
| **`xqa_profile_5060ti/`** | <1 M | XQA vs FlashInfer **latency** sweep (CUDA-event, no profiler), gray sm120 — `xqa_latency.json` | `make_xqa_figure.py` → report 6 |
| **`bench_one_batch_smallkv_5060ti/`** | few M | **Report 8 Tier A** — TRUE-batch `bench_one_batch` at **SMALL KV** (footprint ladder L1→DRAM), Qwen3-VL-2B, CUDA graph ON, FlashInfer{1,2,4,8,16,32,128}+Triton{1,8,32,128} × single-seq B1×{256..32768} & batch B{1..32}×{128,256,512} & high-batch B{64,128,256}×128 × multi-round. Engine TPOT flat (weight-DRAM-bound) → `ps1` tied/fastest. Tags `bobk_r{r}_b{B}_l{L}_{be}_ps{ps}`. | `analyze_bob_smallkv.py`, `make_bob_smallkv_figure.py` → report 8 |
| **`smallkv_profile/`** | <1 M | **Report 8 Tier B** (+ report-9 kernel control) — isolated FlashInfer decode kernel, **distinct KV swept across footprint** (8 MB→537 MB). `lat_distinct_b{1,8,32}.json` (CUDA-event latency, page{1..128}) + `lat_distinct_xqa_b8.json` + ncu `flashinfer_distinct_{all,none}_ps{1,128}_bs8_kv{L}.csv` (**cold=faithful + warm=residency** DRAM%/L2-hit). Distinct KV is DRAM-streamed (cold 79→96%) even when small; `ps1`≈`ps128` in every regime. **`kctrl_*.json`** = report-9 kernel control: the FlashInfer decode KERNEL is page-flat (ps16/ps128 −0.9%) at the B128/L512 footprint → the apparent vLLM +15% is not the kernel (it was a preemption artifact; clean true batches are flat). | `make_smallkv_kernel_figure.py` → reports 8, 9 |
| **`vllm_smallkv_5060ti/`** | <1 M | **Report 9** — vLLM 0.23 TRUE-batch decode TPOT vs `block_size`, **FlashInfer backend, CUDA graph ON, no prefix sharing** (distinct random-token prompts), Qwen3-VL-2B, phastform sm120. `vllm_b{B}_l{L}_blk{blk}.json` (initial sweep) + `probe_blk*` (block 1,8 rejected; 16/32/64/128 OK) + **`fit_*`** (CLEAN re-measure, concurrency≥B) + `vfy_*`/`eag_*`/`clean_*` (reversed-order, graph-OFF, controls). **Verdict: NO clean ≥5% small-block penalty** — clean true batches are flat (≤0.2%); the initial sweep's apparent blk16 +15.3% (B128/L512) was a **KV-pool-overflow preemption** artifact (batch can't fit 16 GB → wave-scheduled), gone once `max concurrency ≥ B`. | `analyze_vllm_smallkv.py`, `make_vllm_smallkv_figure.py` → report 9 |
| **`sharedprefix_profile/`** | <1 M | **Report 7** — ncu (phastform, root) of FlashInfer decode, **shared vs distinct KV** × page{1,128} × {bs8/6144, bs8/39168, bs16/3072}: the DRAM↔cache flip. `flashinfer_{shared,distinct}_ps*_bs*_kv*.{csv,ncu-rep}` + `lat_{shared,distinct}.json` (CUDA-event latency). **`pad2/`** = over-read test (distinct, exact vs partial last page: L∈{1024,1088,6080,6144}) — ncu `dram__bytes_op_read` shows ps128/ps1 ratio ≈0.998 (no padding over-read → ps1 has no real advantage; the true-batch ≤1% is noise). | `make_sharedprefix_figure.py` → report 7 |
| **`sharedprefix_5060ti/`** | <1 M | Report 7 engine cross-check (gray) — `measure_batch_latency_offline.py` radix ON vs `--disable-radix-cache`, eager (`eng_{shar,dist}_*`). Confirms the radix control; strong-signal 10k cells OOM (the bs×ctx tradeoff). | (cross-check; report 7) |
| **`pagecost_profile/`** | <1 M | **Report 10** — decisive FlashInfer true-batch page-cost test (phastform sm120, `sglang_CW`, weightless). `pagecost_gather.json` = decode KERNEL latency at distinct footprints 0.03→8.6 GB with `kv_indices` **contig / block128 / scatter** (worst-case ps1 fragmentation ≤ +1.3% everywhere incl. B8×131072 = 1.05M entries). `pagecost_plan.json` = host `plan()` cost vs index length 64→2.1M entries (**flat ~0.26 ms**, length-insensitive). `flashinfer_{contig,scatter}_all_bs{B}_kv{L}.csv` = **ncu** (cold) of the captured kernel: contig≈scatter at 96% DRAM, 0.7% L2-hit, **identical 15.6 sectors/request**. Confirms `--page-size` only changes `kv_indices` value contiguity (pool token-flat; index length = `B·ctx`, page-independent) and that costs ≤1.3%. + `SUDO_CHANGES.md`. **Also holds Report 11 (MLA caveat):** `mla_pagecost.json` + `mla_worstcase.json` (FlashInfer MLA decode kernel, 1152 B/token latent, h16/h128, contig/block64/scatter — worst-case scatter saturates **+4.07%, <5%**; block64 free) + `mla_h{H}_{contig,scatter}_all_bs{B}_kv{L}.csv` ncu (sectors/request identical → not coalescing; DRAM 15–38% → latency-bound, scattered-page DRAM latency exposed). | `make_pagecost_figure.py` → report 10; `make_mla_pagecost_figure.py` → report 11 |
| **`reuse_profile/`** | <1 M | **Report 12** — why L2-hit ≈0% in distinct true-batch decode: **reuse not residency** (phastform sm120, weightless, 58 ncu cells). `flashinfer_{distinct,shared}_{all,none}_ps{1,128}_bs{B}_kv{L}.csv` over a B∈{1,4,8,16,32}×L∈{1k,4k,8k,16k} grid + a shared reuse sweep B=1→32 at fixed L=1024 (4 MB) + B8 cold/warm distinct ladder + ps1 diagonal. Metrics add L2 hit/miss **sector counts** (`lts__t_sectors_lookup_{hit,miss}.sum`, `..._srcunit_tex_op_read_..`). Findings: L2-hit ≈ **(R−1)/R** (R=reuse=B) at *pinned* footprint (2.6→97%); distinct ≤2% everywhere incl. sub-L2; shared banded by B, flat in L; **warm** distinct peaks 74% sub-L2 = cross-launch **residency artifact** (cold ≈0% = faithful); distinct misses 99.4–99.8% compulsory (miss×32 B ≈ DRAM read). + `SUDO_CHANGES.md`. | `make_reuse_figure.py`, `make_reuse_grid_figure.py` → report 12 |
| `triton_sweep_5060ti/` | 72 K | Triton page sweep, 10k ctx, CUDA graph ON (`tri_10k_g_*`) | `make_v2_figures.py`, `make_dense_figures.py`, `make_v2_extra_figures.py` → reports 2, 3 |
| `triton_clean_5060ti/` | 32 K | Triton, no-graph, clean (`tri_clean_ng_*`) | `make_v2_figures.py`, `make_dense_figures.py` → reports 2, 3 |
| `fi_clean_5060ti/` | 32 K | FlashInfer clean, graph & no-graph (`fi_clean_{g,ng}_*`) | `make_v2_figures.py`, `make_dense_figures.py` → reports 2, 3 |
| `ps_e2e_5060ti/` | 16 K | End-to-end engine TPOT baseline, FlashInfer graph ON (`results_ps*`) | `make_v1_figures.py`, `make_v2*` , `make_dense_figures.py` → reports 1, 2, 3 |
| `ps_nocudagraph_5060ti/` | 20 K | FlashInfer graph ON/OFF, long ctx, bs1 (`ctx*_ps*`) | `make_v2_figures.py`, `make_v2_extra_figures.py` → report 2 |
| `vllm_blocksize_5060ti/` | 76 K | vLLM `block_size` sweep (sm120, FLASH_ATTN backend) | `make_models_figures.py`, `make_v2_extra_figures.py` → report 3 |
| `H100-80GB-HBM3/` | 1.2 M | **Committed** reference data (Qwen3-VL-32B input-length sweep) — *not part of this study's new data* | — |

## Small local data (in `page_size_study/data/`)

| file | what |
|---|---|
| `page_size_attn_bench_5060ti.json` | Isolated FlashInfer decode-attention kernel microbench, RTX 5060 Ti (report 1) |
| `confirm_A_5060ti.json` | Warm re-measurement of the bs1/kv1024 cell (corrects a cold-start artifact; report 1) |
| `noise_test/{fwd,rev}_*.json` | Forward/reverse page-order noise control (report 1, Fig 5) |
| `dense_consistency.md` | Self-consistency table emitted by `make_dense_figures.py` |

(The datacenter kernel bench `page_size_attn_bench.json` referenced by report 1 is a pre-existing repo-root file, not part of this study.)
